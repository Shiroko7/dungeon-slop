[package]
name = "dungeon-core"
version = "0.1.0"
description = "Deterministic dungeon generation. Port of the TypeScript engine in dungeon-slop."
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]

[dev-dependencies]
serde_json = "1"
proptest = "1"

[lib]
crate-type = ["rlib", "cdylib"]
